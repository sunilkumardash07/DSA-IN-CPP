#include <iostream>
using namespace std;
int main() {
    //Memory Allocation in Arrays ==>Continuous(consecutive) Memory Allocation in Arrays
    //Pros == accessing array element takes O(1) time (ek hi baar mein ho jata hai)
    //Address of Array Elements
    
    // int arr[4];
    
    int arr[]={1,23,4,56,78,-90};
    int n= sizeof(arr)/4;
    cout<<arr<<endl;//only arr gives the first element address(gate of the society)
    cout<<&arr[0]<<endl;//These two are same 
    cout<<&arr[1]<<endl;//Difference of 4
    cout<<&arr[3]<<endl;
    
}
