#include <iostream>
#include <climits>
#include <cfloat>
using namespace std;
int main(){
    //Second Maximum
    int n;
    cout<<"Enter The Number of Elements in Array= ";
    cin>>n;
    float arr[n];
    for(int i=0;i<=n-1;i++){
        cout<<"Enter The Number("<<i<<")= ";
        cin>>arr[i];
    }
    
    cout<<"Array = {";
    for(int i=0;i<=n-1;i++){
        if(i==n-1) cout<<arr[i]<<"}";
        else cout<<arr[i]<<",";
    }
    cout<<endl;
    
    float mx= -FLT_MAX;
    for(int i=0;i<=n-1;i++){
        if(arr[i]>mx) mx= arr[i];
    }
    
    float smx= -FLT_MAX;
    for(int i=0;i<=n-1;i++){
        if(arr[i]>smx && arr[i]!=mx) smx= arr[i];
    }
    cout<<"Second Maximum Element in The Array= "<<smx;
}
