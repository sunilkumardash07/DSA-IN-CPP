#include <iostream>
using namespace std;
int main() {
	int n;
	cout<<"Enter The Number= ";
	cin>>n;
	if(n%5==0 && n%3==0)cout<<"It is divisble by both 5 and 3";//isse pehle likhne se farak pdta hai
	else if(n%5==0 || n%3==0) cout<<"It is divisible by either 5 or 3";
	else cout<<"It is not divisible by 5 or 3";






}
