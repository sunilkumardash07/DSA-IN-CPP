#include <iostream>
using namespace std;
int main() {
	//Ques. Take Positive integer inputb and print
	// if no is divisible by 5
	//if no is divisible by 3
	//if no. is divisible by 5 and 3 both
	//if no. is not divisible by 5 or 3
	int n;
	cout<<"Enter The Number= ";
	cin>>n;
	if (n%5==0 and n%3==0)cout<<"It is divisible by both 5 and 3  ";
	else if(n%5==0)cout<<"It is divisible by 5 only";
	else if(n%3==0)cout<<"It is divisible by 3 only";
	else cout<<"It is not divisble by both 5 or 3";

//want to use only if if if if then use not equals to





}
