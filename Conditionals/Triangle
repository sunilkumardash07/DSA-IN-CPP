#include <iostream>
using namespace std;
int main() {
	//Question. Take 3 positive integers input and tell if they can be the sides of a triangle or not.
	int x;
	int y;
	int z;
	cout<<"Enter length of side(x)= ";
	cin>>x;
	cout<<"Enter length of side(y)= ";
	cin>>y;
	cout<<"Enter length of side(z)= ";
	cin>>z;

	if(x+y>z && x+z>y && y+z>x)cout<<"It can form a Triangle";
	else cout<<"It cannot form a triangle";






}
