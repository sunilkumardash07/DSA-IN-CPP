#include <iostream>
using namespace std;
int main() {
	int a,b;
	cout<<"Enter the Base= ";
	cin>>a;
	cout<<"Enter the Exponent= ";
	cin>>b;

	int x=1;
	for(int i=1; i<=b; i++) {
		x*=a;
		if(a==1)break;
	}
	if(a==0 && b==0)cout<<"Indeterminate Form";
	cout<<"Answer= "<<x;

}
