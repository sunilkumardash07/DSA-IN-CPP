#include <iostream>
using namespace std;
//Swap Using Pointers
void swap(int* p1, int* p2) {
	int temp=*p1;
	*p1=*p2;
	*p2=temp;

}
int main() {
	int a,b;
	cout<<"Enter Two Numbers= ";
	cin>>a>>b;
	cout<<"Swap= ";
	swap(&a,&b);
	cout<<a<<" "<<b<<endl;
}
